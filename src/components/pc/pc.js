import { h, render, Component } from "preact";

export default class GoPC extends Component {
  constructor(props) {
    super();
  }
  render({}, {}) {
    return (
      <div>
        <a style="color:white;" href="http://muxi-tech.xyz">
          电脑端请观看pc端
        </a>
      </div>
    );
  }
}
